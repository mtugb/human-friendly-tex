[package]
name = "lsp"
edition = "2024"
version.workspace = true # workspaceのバージョンを適応

[dependencies]
tokio = { version = "1.50.0", features = ["full"] }
tower-lsp = "0.20.0"
mytex = { path = "../core" }
